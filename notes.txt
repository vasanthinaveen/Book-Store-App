Dependencies:
-------------

    1.spring web
    2.Spring boot  Data Mongo DB
    3.Spring boot dev tools  - whenever we make changes i dont need to restart.
    4.Docker compose  it will help me get mongodb image.i dont need to install mongodb
(Docker compose support is available in spring boot through docker image for mongodb iam doing this project)
    if we include docker dependency it will get us compose.xml

    Java 17
    ----------------------------------
    Run the application  now you can see the bookstore and mongodb image in docker.

    I f we include MONGODB in dependencies then it will automatically create comose.yaml
    I f we include MONGODB in dependencies then it will automatically create comose.yaml

    1.we created a ccontroller class:
    wrote @Restcontroller,@Restmapping,and get,put,post,delete   mapping to expose the api.
    2.we created a java record  -- immutable-- to create an entity.
             in the record we give the fields needs to be created .then the rest will be taken care automatically.
                getter will be autogenerated.but no setter,since it is immutable.
                entity.book
    3. create DTO object-- same as entity record.name it as DTO.bookDto

    entity will be --talking to database
    dto talking to rest api
    these both talk to each other.

