services:
  mongodb:
    image: mongo:latest
    container_name: latestbook1
    ports:
      - "27020:27017"
#    environment:
#      MONGO_INITDB_ROOT_USERNAME: root
#      MONGO_INITDB_ROOT_PASSWORD: secret
